# 기여 가이드

## 목표

이 스킬은 대상 게임의 증거를 바탕으로 한글 패치의 다음 행동을 선택하고, 같은 입력과 증거에서 판정을 재현할 수 있게 한다.

플레이어는 보통 한 번만 플레이한다. 배포 결과에는 크래시와 진행 불가뿐 아니라 깨진 글자, 누락된 글리프, 틀린 힌트·아이템명, 무너진 용어·말투와 조용한 인코딩 손실도 남기지 않는다.

문서에는 두 종류의 가치가 있다.

- **실시간 추론으로 복원할 수 있는 지식:** 일반적인 사용 예시, 쉽게 유도되는 공식, 명확한 1차 자료 한 곳에서 확인되는 기본 사양
- **저장된 노동:** 실제 대상에서 검증한 사실, 여러 실패 뒤 입증한 기법, 충돌하는 자료를 소비자·실험으로 가른 결론, 경험적 함정

기여는 저장된 노동을 보존하고, 국소 결론에는 관측 범위와 새 대상에서 다시 확인할 경계를 붙인다.

## 문서 배치

| 위치 | 내용 |
|---|---|
| `skills/create-kr-patch/SKILL.md` | 현재 판단에서 첫 참조문서로 가는 경로와 전 영역의 핵심 불변식 |
| `references/strategy/` | 발동 조건, 필요한 증거, 판정 기준과 다음 행동 |
| `references/conventions/` | 저장소·산출물·교환 데이터·기록이 보존할 의미와 실패 조건 |
| `references/platforms/` | 하드웨어·매체·주소공간·렌더링 경로에서 현재 분기를 바꾸는 제약 |
| `references/tips/` | 범위가 확인된 실패·성공 기법, 운영 함정과 어려운 교차 조사 결과 |

프로젝트별 주소·오프셋·파일명·실행 결과와 진행 기록은 해당 프로젝트에 둔다. 도구·언어·라이브러리·폰트는 필요한 능력과 검증 기준을 충족하는 범위에서 대상 프로젝트가 선택한다.

명확한 1차 자료 한 곳에서 확인되는 기본 사양은 작업 시점에 조사한다. 여러 자료의 적용 범위를 교차하거나 실제 소비자·반례·재현 실험으로 판정한 지식은 이 스킬에 보존한다. 안정된 플랫폼 제약은 `references/platforms/`, 타이틀·도구·버전·입력 범위에 묶인 결과는 `references/tips/`에 둔다.

여러 프로젝트에서 반복되어 실패를 막은 능력은 구현 이름을 걷어내고 공통 조건으로 남긴다. 보호된 원문과 가역 토큰을 쓰는 번역 배치, 독립 검토와 사람 승인, 전체 ISA 검산, Expected Write의 원본·범위·충돌 검증이 이에 해당한다.

## 문장과 참조

각 문장과 표는 다음 중 하나를 수행한다.

1. 다음 행동이나 가설을 바꾼다.
2. 성공·실패·완료를 판정한다.
3. 쉽게 복원할 수 없는 검증 노동을 보존한다.
4. 관측 가능한 조건에서 필요한 문서나 절로 연결한다.

판정 기준을 먼저 쓰고, 완성 가능성·원본 보호·사람의 승인 권한·배포 안전을 깨뜨리는 행위만 명시적으로 금지한다. 문서를 읽는 방법, 편집 의도와 예상 오독을 해명하는 문장은 구조와 직접적인 발동 조건으로 바꾼다.

다른 문서의 기준을 적용할 때는 `references/conventions/project-conventions.md`처럼 저장소 기준 전체 경로를 쓴다. 긴 문서의 특정 판정이 필요하면 절 제목이나 `§N`을 함께 적는다. 같은 기준을 여러 문서에 복사하지 않는다.

본문에는 작성자·날짜·확인일·변경 이력과 이동 흔적을 남기지 않는다. 문서는 현재의 사실을, git은 변경 이력을 보존한다.

문서 이동과 절 번호 변경은 포인터와 `scripts/reference-sections.json`을 함께 갱신한다. 다음 명령으로 파일·앵커·절 제목과 tips 색인 대응을 검사한다.

```bash
python3 scripts/validate_docs.py
```

## 전략 문서

전략은 다음 질문에 답한다.

- 어떤 관측이나 위험에서 발동하는가?
- 어떤 증거가 선택지를 가르는가?
- 결과별로 무엇을 확정하고 다음에 무엇을 하는가?
- 어느 범위까지 완료로 판정하는가?

조사 순서와 수단은 현재 불확실성에 맞춰 선택한다. 선언한 완성 범위에서 실패하면 완성이 불가능하거나 설계를 크게 바꾸는 조건을 먼저 판정하고, 같은 조건을 가르는 증거 사이에서만 비용을 비교한다. 서로 독립된 경계는 병렬로 조사하고, 새 증거가 바꾼 판정만 원인 경계로 되돌린다.

PoC는 설계를 바꿀 수 있는 위험을 조건부로 검증한다. 가시성, 인코딩 공간, 글리프 수용량, 재배치, 압축과 런타임 소비는 각각의 증거 범위에서 판정한다.

번역과 재삽입은 글리프·길이·레이아웃 수요를 다시 설계로 돌려보낼 수 있다. 공급 확대 가능성을 먼저 판정하고, 의미·말투 손실이 있는 축소는 사람이 승인한다. 전체 배포 범위의 사람 검수 시점은 `references/strategy/translation-workflow.md`를 따른다.

## 플랫폼과 검증된 사례

플랫폼 문서에는 현재 판단을 바꾸는 사실과 적용 범위를 함께 쓴다. 주소 변환식·용량·트랙 배치·글리프 슬롯·빈 공간·포인터 폭은 실제 매핑 모드·매체·소비자에서 확인한 범위를 표시한다.

특정 타이틀에서 검증한 기법과 실패는 `references/tips/`에 둔다. 다음 중 하나에 해당하고 다시 얻는 데 검증 노동이 드는 사례를 보존한다.

- 틀린 가정과 원인을 가른 반례 또는 결정 실험
- 실제 빌드와 소비 경로에서 입증된 성공 기법
- 여러 자료의 충돌을 소비자나 재현으로 판정한 결과
- 특정 도구·버전·입력 조합에서 드러난 운영 함정

사례 제목과 필드는 에이전트 검색을 위해 영어로 쓴다. 제목은 플랫폼 코드가 아니라 재사용할 결론을 직접 말하며, 각 사례는 다음 정보를 제공한다.

- **Search terms:** 증상·자료 구조·원어 기술 용어를 포함한 검색 별칭
- **Observed scope:** 결론을 제한하는 플랫폼·타이틀·리비전·도구·입력
- **Failure context** 또는 **Decision context:** 사례가 필요했던 실패나 선택
- **Decisive test** 또는 **Evidence:** 원인을 가른 실험·대조군·현 코드·교차 자료
- **Established result:** 기각 결론, 확정 원인 또는 실제로 통과한 개입
- **Transfer limit:** 새 대상에서 다시 확인할 전제와 그대로 옮길 수 없는 수치·좌표·선택
- **Related criteria:** 사례를 선택하고 완료를 판정할 strategy 문서

결론의 적용에 특정 플랫폼 규칙이 필요하지 않으면 `references/tips/general/`에, 그 규칙이 필요하면 `references/tips/platforms/`에 둔다. 색인은 판단 영역·관측 증상·최초 관측 범위·사례 앵커를 연결하며, 같은 사건은 한 사례 파일에만 기록한다.

## 구현과 검증

반복 빌드와 검증에 들어오는 구현은 다음 조건을 충족한다.

- 입력과 출력, 원본 식별 정보와 실패 조건을 명시한다.
- 원본부터 최종 산출물까지 같은 경로로 다시 실행할 수 있다.
- 자동 판정 가능한 라운드트립·체크섬·변경 범위·글리프 누락·레이아웃 초과를 빌드에서 검사한다.
- 대화형 조사가 필요하면 입력·상태·관측 결과와 다음 조사 시작점을 기록한다.
- 원본 ROM·디스크 이미지와 허가되지 않은 제3자 자산은 저장소와 배포물에서 제외한다.

## 리뷰

- 현재 판단과 다음 행동이 선명한가?
- 저장된 노동이 보존됐는가?
- 한 사례의 결론이 일반 규칙으로 확대되지 않았는가?
- 쉽게 확인할 기본 사양을 반복하지 않았는가?
- 문장마다 행동·판정·증거·라우팅 가운데 하나가 있는가?
- 같은 기준이 다른 문서에도 반복되는가?
- 포인터가 실제 파일과 절을 가리키는가?
- 한국어가 직접적이고 자연스러운가?
- 플레이어 경험을 해칠 실패가 빌드나 실행 검증에서 발견되는가?
