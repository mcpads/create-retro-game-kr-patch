# 기여 가이드

## 기여 철학

이 스킬의 목표는 사람이 휴리스틱하게 개입해야만 진행되는 한글 패치 절차를 줄이고, Agent가 대상 게임의 증거를 바탕으로 스스로 다음 행동을 선택하게 만드는 것이다.

레트로 게임 한글 패치는 단순한 파일 수정이 아니라 플레이어의 경험을 결정한다. 플레이어는 보통 한 번만 플레이하므로, 배포 가능한 결과의 기준은 "대체로 동작한다"가 아니라 "알려진 나쁜 일이 없다"이다. 크래시, 진행 불가, 깨진 글자, 누락된 글리프, 틀린 힌트·아이템명, 무너진 말투, 조용한 인코딩 손실은 모두 블로커다.

AI 기반 패치의 약점은 그럴듯하지만 검증되지 않은 결과를 만들 수 있다는 점이다. 이 스킬은 그 위험을 재현성, 문서화, 라운드트립 검증, 에뮬레이터 검증, 빌드 실패 조건으로 줄인다. 좋은 기여는 Agent가 이전 작업자를 모르는 상태에서도 같은 입력과 증거를 따라가고, 같은 판정을 재현할 수 있게 만든다.

LLM/Agent의 장점은 게임마다 다른 상황에 맞춰 능동적으로 판단할 수 있다는 점이다. 따라서 문서는 모든 상황을 고정 절차로 박아 넣기보다, Agent가 판단할 수 있는 게이트, 증거, 실패 신호, 다음 행동의 기준을 제공해야 한다. 사람에게 "이럴 땐 알아서 판단"을 요구하는 문서는 좋은 문서가 아니다.

대화형 조작만을 유일한 재현 경로로 두지 않는다. UI가 필요한 조사도 입력·상태·관측 결과와 다음 진입점을 남기고, 가능한 판정은 테스트·로그·체크섬처럼 다시 확인할 수 있는 증거로 바꾼다.

좋은 기여는 사람의 개입을 줄인다. 좋은 기여는 대상 게임에 맞는 판단을 가능하게 한다. 좋은 기여는 한 사례의 감각을 일반 규칙으로 굳히지 않는다. 좋은 기여는 "무엇을 했다"보다 "그 결과 어떤 불확실성이 줄었고, 다음에 무엇을 할 수 있게 되었는가"를 남긴다.

문서가 주는 가치는 추론 시점에 다시 만들 수 있는 실행 유도와, 과거에 검증 비용을 치른 **저장된 노동**으로 나뉜다. 일반적인 사용 예시, 쉽게 유도되는 공식과 쉽게 접근 가능한 명확한 1차 자료 한 곳에서 복원되는 사양은 전자에 가깝다. 실제 대상에서 검증한 사실, 여러 실패 뒤 입증한 기법, 서로 충돌하는 자료를 소비자·실험으로 가른 결론과 경험적 함정은 후자다. 국소적이라는 이유만으로 저장된 노동을 버리지 않으며, 일반 규칙으로 과장하지 않고 관측 범위와 전이 한계를 붙여 보존한다.

## 문서 티어

문서는 추상도에 맞는 곳에 둔다.

- `SKILL.md`는 헌법 티어다. 프로젝트가 바뀌어도 유지되어야 하는 원칙, 판단 영역 간 의존, 참조문서 라우팅, 불변식만 둔다.
- `references/strategy/`는 전략 티어다. 어떤 상황에서 어떤 게이트를 적용하고 어떤 기준으로 통과 판정할지, 성공·실패·불명확을 어떻게 판정하는지, 다음 행동을 어떻게 고르는지 설명한다.
- `references/conventions/`는 시행 규약 티어다. 저장소 역할, 보호 경계, 데이터·기록이 보존할 의미와 검증 규칙을 둔다. 경로·스키마·직렬화 형식 자체를 표준화하거나 새로운 전략·통과 기준을 만들지 않는다.
- `references/platforms/`는 플랫폼 사실과 대상 분기를 바꾸는 제약을 둔다. 하드웨어·매체·주소공간·렌더링 경로 가운데 실제 판정에 필요한 범위만 쓰며, 여러 자료를 교차해 확정한 안정된 플랫폼 제약도 이 범주에 포함한다.
- `references/tips/`는 비규범적 검증 사례 티어다. 실패·반례뿐 아니라 범위가 확인된 성공 기법, 운영 함정과 어려운 교차 조사 결과를 보존한다. strategy는 tips 없이도 완결되어야 하며, Agent는 서로 독립된 발동 조건마다 판단 영역과 조건에 가장 직접 맞는 사례 하나를 선택하고 관측 범위를 전이 한계로 확인한다.
- 프로젝트별 주소, 오프셋, 파일명, 도구 실행 결과, 진행 기록은 해당 프로젝트 문서의 책임이다. 이 스킬의 상위 문서에 로컬 작업 기록을 끌어올리지 않는다.
- 도구·언어·라이브러리·폰트 후보 목록과 버전 종속 명령은 스킬에 고정하지 않는다. 스킬은 필요한 능력·제약·검증 기준을 남기고, 구체 선택은 대상 프로젝트의 기존 도구체인과 현재 환경을 조사해 결정한다.
- 공개 검색으로 곧바로 재구성할 수 있는 후보 카탈로그와 일반 배경지식을 분량 채우기처럼 싣지 않는다. 외부 사실은 Agent의 분기·실패 판정·검증을 바꾸는 최소 제약만 남긴다.
- 쉽게 접근 가능한 명확한 1차 자료 한 곳에서 복원되는 기본 사양은 작업 시점의 조사에 맡긴다. 여러 출처의 적용 범위를 교차해야 하거나 서로 충돌하는 설명을 실제 소비자·반례·재현 실험으로 판정해야 얻는 지식은 저장된 노동으로 취급한다. 안정된 플랫폼 제약이면 `references/platforms/`, 타이틀·도구·버전·입력 범위에 묶이면 `references/tips/`에 둔다.
- 여러 실제 프로젝트에서 반복 구현되어 실패를 막은 공통 능력은 시행 형태를 가진다는 이유만으로 버리지 않는다. Agent 번역 배치의 보호 입력은 `references/strategy/translation-workflow.md`와 `references/conventions/translation-artifacts.md`, 기계어 생성·재해석 검산과 Expected Write 충돌·범위 검증은 `references/conventions/project-conventions.md` §2.3·§5.2가 소유한다. 고정 batch 수, 특정 라이브러리·명령과 한 프로젝트의 배치는 함께 올리지 않는다.
- 구체 도구·버전·입력이 실패를 결정한 경우 그 이름은 `references/tips/`의 관측 범위와 재검증 조건에만 쓸 수 있다. 이를 공통 도구 선택이나 영구 처방으로 승격하지 않는다.

본문에는 작성자, 확인일, 변경 이력, 업데이트 배너를 넣지 않는다. 문서는 현재의 사실만 담고, 이력은 git이 보유한다.

## 문서 간 참조 포인터

상위 티어에서 구체적인 시행 내용을 설명해야 할 때는 그 내용을 복제하지 않고 소유 문서로 포인팅한다.

- 전략 문서에는 적용 조건, 판단 근거, 통과 기준과 다음 행동만 남긴다. 프로젝트가 채택한 표현을 언급해야 하면 `references/conventions/`의 소유 문서를 전체 저장소 상대 경로로 가리킨다.
- 플랫폼 문서에는 플랫폼 사실과 적용 범위가 표시된 사례만 남긴다. 공통 추출·재삽입·검증 방법론은 `references/strategy/`를, 프로젝트별 좌표와 실행 기록은 해당 프로젝트 문서를 가리킨다.
- tips는 관련 strategy를 가리키되 strategy가 tips에 의존하게 만들지 않는다. tips 색인은 본문을 복제하지 않고 판단 영역·발동 조건·관측 플랫폼·대상 앵커만 제공하며, Agent는 서로 독립된 발동 조건마다 판단 영역과 조건에 가장 직접 맞는 사례 하나만 읽고 관측 범위를 전이 한계로 확인한다.
- 탐색을 위한 포인터는 `project-conventions.md`처럼 파일명만 줄여 쓰지 않고 `references/conventions/project-conventions.md`처럼 전체 경로를 쓴다. 필요한 경우 절 제목이나 `§N`을 함께 적는다.
- 같은 규칙·표·절차를 두 문서에 복사하지 않는다. 한 문서만 내용을 소유하고 나머지는 한 문장짜리 맥락과 포인터만 둔다.
- 문서를 이동하거나 절 번호를 바꾸는 변경은 기존 포인터를 같은 커밋에서 갱신하고, 대상 파일과 절이 실제로 존재하는지 검증한다.

`python3 scripts/validate_docs.py`는 참조 파일·앵커·절의 존재, 절 번호에 고정한 기대 제목과 tips 색인의 1:1 대응을 검사한다. 절을 추가·이동·재번호하면 포인터와 `scripts/reference-sections.json`의 기대 제목을 같은 커밋에서 갱신한다.

## 좋은 기여의 기준

좋은 기여는 다음 조건을 만족한다.

- **사람 개입을 줄인다.** 사람이 판단해야 했던 휴리스틱을 Agent가 재현 가능한 증거와 절차로 처리할 수 있게 만든다.
- **상황 맞춤형 판단을 가능하게 한다.** 고정된 정답을 강요하지 않고, 게임마다 다른 결과를 판정할 수 있는 질문·증거·게이트를 제공한다.
- **이전 대화와 작성자의 기억에 의존하지 않는다.** 다른 에이전트가 문서만 읽고 같은 조사를 이어갈 수 있어야 한다.
- **재현 가능하다.** 입력·버전·체크섬·로그·테스트·실행 증거처럼 같은 판정을 다시 확인할 수 있는 형태로 남긴다.
- **플레이어 경험을 보호한다.** "화면에 보인다"가 아니라 알려진 나쁜 일이 없도록 빌드 실패 조건과 검증 게이트를 강화한다.
- **다음 행동을 개선한다.** 성공, 실패, 불명확 어느 결과가 나와도 다음 선택지가 더 정확해지도록 작업 단위를 설계한다.
- **사례를 가설로 취급한다.** 선행 사례는 단서이지 규칙이 아니다. 새 게임에서는 다시 측정하고, 일반화할 때는 범위를 좁혀 쓴다.

## 전략 문서 기여

전략 문서를 보강할 때는 절차 목록만 늘리지 말고 판정 기준을 함께 쓴다.

- 언제 이 전략이 발동하는지 쓴다.
- 어떤 증거를 수집해야 하는지 쓴다.
- 성공, 실패, 불명확의 차이를 쓴다.
- 성공하면 어떤 지식이 승격되는지 쓴다.
- 실패하면 어떤 가정을 버리는지 쓴다.
- 불명확하면 어떤 더 작은 확인으로 쪼갤지 쓴다.

PoC는 성취감용 데모가 아니라 조건부 게이트 묶음이다. 목표 렌더 경로의 한글 픽셀 도달이 아직 확인되지 않았을 때 한글 1자 출력이 가시성 게이트가 될 수 있지만, 인코딩 공간, 전체 글리프 수용량, 재배치 가능성, 런타임 버퍼, 이름 입력, 그래픽 텍스트 경로까지 자동으로 증명하지 않는다. 이미 동등한 증거가 있는 게이트는 반복하지 않으며, PoC 문서는 어떤 위험을 검증했고 무엇이 남았는지 분리해서 써야 한다.

번역과 재삽입은 피드백 루프다. 글리프·길이 수요가 공급을 넘으면 실제 병목과 공급 변경 가능성을 먼저 판정한다. 의미·말투를 잃는 번역 축소는 공급 제약이 해소되지 않고 사람이 그 손실을 승인한 경우에만 채택한다. 초벌 코퍼스의 역할과 전체 배포 범위의 사람 검수 시점은 `references/strategy/translation-workflow.md`에서 정한다.

## 플랫폼 문서 기여

플랫폼 문서는 게임별 경험담을 일반론처럼 굳히는 곳이 아니다. 플랫폼 문서에 새 내용을 넣을 때는 다음을 구분한다.

- 공개 하드웨어·포맷 사실
- 적용 조건과 반복 근거가 확인된 플랫폼 패턴
- 특정 게임에서만 검증된 실패·성공 기법·조사 결과 — platform 본문이 아니라 `references/tips/` 후보
- 아직 가설인 위험 신호

공개 하드웨어·포맷 사실이라도 쉽게 접근 가능한 명확한 1차 자료 한 곳으로 충분하면 플랫폼 문서에 복제하지 않는다. 단일 자료만으로는 적용 범위를 결정할 수 없거나, 여러 계층·표현을 혼동하면 잘못된 빌드 분기로 이어지는 사실만 그 판정 경계와 함께 남긴다.

수치에는 적용 범위를 붙인다. 주소 변환식, 용량, 트랙 배치, 글리프 슬롯 수, 빈 공간 크기, 포인터 폭은 매핑 모드·매체 형식·게임 구조에 따라 달라질 수 있다. 한 사례의 값을 "항상" 또는 "표준"으로 쓰지 않는다.

플랫폼 본문의 제약을 별도 요약 절에서 반복하지 않는다. 타이틀의 검증 사례를 보존할 필요가 있으면 `references/tips/`로 분리한다. 프로젝트의 세부 진행 기록, 임시 작업 경로, 확인일은 쓰지 않는다.

## 검증된 국소 사례 기여

다음 중 하나에 해당하고, 다시 얻는 데 실제 검증 노동이 드는 경우만 `references/tips/`에 추가한다.

- 틀린 가정과 원인을 가른 반례 또는 결정 실험
- 대안과 비교해 실제 빌드·소비 경로에서 입증된 성공 기법
- 여러 자료의 충돌을 실제 소비자나 재현으로 판정한 조사 결과
- 특정 도구·버전·입력 조합에서만 드러난 운영 함정

각 사례는 최소한 다음을 복원할 수 있어야 한다.

- **관측 범위:** 플랫폼·타이틀·리비전·도구·입력 가운데 결론을 제한하는 범위
- **문제·선택 맥락:** 어떤 실패나 선택 때문에 이 사례가 필요했는지
- **검증 근거:** 원인을 가른 실험, 대조군, 현 코드 또는 다중 자료 교차 판정
- **확정 결과:** 기각 결론, 확정 원인 또는 실제로 통과한 개입. 진행률·적용 건수·후속 과제는 인과나 전이 한계를 바꾸는 경우가 아니면 결론에 넣지 않는다.
- **전이 한계:** 새 대상에서 다시 확인해야 할 전제와 이식하면 안 되는 수치·좌표·도구 선택
- **관련 판단 기준:** 현재 사례를 후보로 선택할 strategy 포인터
- **재검증 조건:** 도구·버전·외부 구현처럼 시간이 지나면 바뀔 수 있는 사례에만 추가

중간 조사 기록과 과거 작업 일지는 당시 가설의 증거일 뿐 최종 결론을 대신하지 않는다. 현 코드, 후속 상태 문서와 커밋까지 대조해 해결 여부와 실제 채택 경로를 복원한다. 일반적인 명령문만 남거나, 공개 자료 한 번으로 복원되거나, strategy를 되풀이하거나, 최종 판정을 확인할 수 없는 항목은 넣지 않는다.

대상 게임의 저장·소비 문법을 알아야 발동 조건을 기술할 수 있으면 해당 플랫폼 사례 파일에 둔다. 증거의 출처·적용 시점, 파생물의 갱신, 실험 설계, 도구와 실제 소비자의 차이, 번역·검토 맥락이 핵심이면 `references/tips/general.md`에 둔다. 같은 사건을 두 파일에 중복하지 않는다.

## 구현과 검증 기여

반복 빌드나 검증에 들어오는 구현은 Agent가 독립적으로 재현할 수 있어야 한다.

- 입력과 출력이 명확해야 한다.
- 실패 시 조용히 넘어가지 말고 에러로 종료해야 한다.
- 원본 ROM·디스크 이미지와 허가되지 않은 제3자 자산을 저장소에 넣지 않아야 한다.
- 라운드트립, 체크섬, 변경 섹터, 글리프 누락, 레이아웃 초과처럼 자동 판정 가능한 결과를 우선해야 한다.
- 대화형 절차가 필요하면 입력·상태·관측 결과와 후속 판정 경로를 문서화한다.

## 피해야 할 기여

- 한 게임에서 관찰한 구조를 플랫폼 일반 규칙처럼 쓰는 기여
- "사람이 보고 적당히 판단한다"를 기본 절차로 두는 기여
- GUI 조작 순서를 유일한 재현 경로로 만드는 기여
- 한글 1자 출력만으로 글리프 예산·인코딩 공간·재배치 가능성을 증명했다고 쓰는 기여
- 검증하기 쉬운 위험만 증명하고 실제 블로커는 우회한 PoC를 위험이 줄었다고 쓰는 기여
- 실패 조건 없이 누락 문자를 대체하거나 건너뛰는 기여
- 작성자, 날짜, 변경 이력, 확인일을 문서 본문에 넣는 기여
- 원본 ROM·디스크 이미지나 허가되지 않은 제3자 자산을 포함하는 기여

## 리뷰 체크리스트

기여 전 스스로 확인한다.

- 이 변경이 사람의 개입을 줄이는가?
- Agent가 이전 맥락 없이도 재현할 수 있는가?
- 대상 게임에 맞춘 판단을 가능하게 하는가?
- 사례와 일반 규칙이 분리되어 있는가?
- 공개 자료의 나열이 아니라 Agent의 분기나 검증을 바꾸는 제약을 남겼는가?
- 상위 티어가 구체 시행을 복제하지 않고 명시적인 전체 경로로 소유 문서를 가리키는가?
- 성공·실패·불명확의 다음 행동이 달라지는가?
- 플레이어 경험을 해칠 수 있는 실패가 빌드나 검증에서 잡히는가?
- 문서 본문에 변경 이력, 작성자, 날짜, 확인일 같은 메타 정보가 들어가지 않았는가?
