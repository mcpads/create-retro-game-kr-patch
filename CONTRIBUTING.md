# 기여 가이드

이 저장소는 레트로 게임 한글 패치 작업에서 반복해서 도움이 된 판단 기준과 검증 경험을 모은다. 에이전트가 따라야 할 순서를 세세하게 정하거나 특정 프로젝트의 해법을 모든 게임에 적용하려는 문서가 아니다.

## 우리가 중요하게 보는 것

- 플레이어는 패치를 통째로 경험한다. 지원 범위 안에 알려진 문제가 남아 있다면 다른 화면의 성공으로 덮지 않는다.
- 확인한 만큼만 말한다. 국소 테스트나 화면 하나의 성공을 프로젝트 전체의 진전으로 부풀리지 않는다.
- 완성된 패치에서 함께 맞아야 할 조건을 먼저 본다. 가장 어려운 미해결 조건과 재설계를 부를 위험을 계속 드러내 둔다.
- 조사 순서를 미리 고정하지 않는다. 증거에 따라 방법을 고르고, 같은 의문만 되풀이되거나 예외가 늘면 전제와 문제 범위를 다시 살핀다.
- 에이전트는 기술적 사실과 실행 가능한 선택지를 밝힌다. 사람은 제품 범위, 품질 기준, 허용할 손실, 지원 대상, 큰 기술 투자를 결정한다. 이미 정한 방향을 그대로 구현하는 일상적인 선택은 에이전트가 맡는다.

사람이 승인했다고 해서 원문 손실, 잘못된 인코딩, 깨진 산출물을 허용해도 되는 것은 아니다. 번역의 의미와 말투는 자동 검사만으로 보장할 수 없으므로 초안, 별도 검토, 사람의 판단, 배포 준비 상태를 구분한다. 자세한 기준은 `skills/create-kr-patch/SKILL.md`와 연결된 전략 문서가 맡는다.

## 무엇을 남길까

한 번 검색하면 나오는 기본 사양보다 다시 알아내기 비싼 지식을 남긴다.

- 실제 대상에서 확인한 구조와 제약
- 여러 가설을 가른 관측이나 실험
- 실제 빌드와 소비 경로에서 확인한 방법
- 반복해서 잘못 판단하기 쉬운 경계와 반례

특정 주소, 파일명, 실행 결과와 현재 진행 상황은 해당 게임 프로젝트에 둔다. 이 저장소에는 다른 프로젝트에서도 판단을 바꿀 수 있는 조건과 새 대상에서 다시 확인해야 할 범위만 남긴다.

## 어디에 쓸까

| 위치 | 맡는 내용 |
|---|---|
| `skills/create-kr-patch/SKILL.md` | 스킬의 핵심 원칙과 세부 문서 안내 |
| `skills/create-kr-patch/references/strategy/` | 언제 살펴볼지, 어떤 증거가 선택지를 가르는지, 결과별 다음 행동과 완료 범위 |
| `skills/create-kr-patch/references/conventions/` | 저장소 구조, 데이터, 기록, 빌드와 검증이 지켜야 할 계약 |
| `skills/create-kr-patch/references/platforms/` | 하드웨어와 매체 때문에 판단이 달라지는 조건 |
| `skills/create-kr-patch/references/tips/` | 특정 대상에서 확인한 실패나 성공과 그 적용 한계 |

기존 문서가 같은 책임을 맡고 있다면 새 문서를 만들지 말고 그 문서를 고친다. 같은 기준을 여러 곳에 복사하지 않는다.

## 문서 쓰는 법

에이전트가 읽는 `SKILL.md`와 `references/` 문서는 영어로 쓴다. 다만 `SKILL.md`의 frontmatter에는 한국어 요청을 인식하기 위한 표현을 남길 수 있다. 사람에게 설치와 기여 방법, 변경 내역을 알리는 루트 `README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`는 자연스러운 한국어로 쓴다. 원문 문자열, 코드 이름, 표준 기술 용어처럼 번역하면 뜻이 달라지는 표현은 그대로 둔다.

문장을 쓰기 전에 실제 작업에 어떤 도움이 되는지 묻는다. 다음 행동이나 판단을 바꾸지 않고, 성공과 실패를 판정하는 데도 쓰이지 않으며, 다시 얻기 비싼 증거도 남기지 않는 설명은 지운다.

`must`와 `must not`은 어겼을 때 실제로 작업이나 배포가 실패하는 조건에만 쓴다. 여러 방법이 가능하면 필요한 결과와 판단 기준을 쓰고 선택은 에이전트에게 맡긴다. 다른 문서를 가리킬 때는 전체 상대 경로와 필요한 절을 함께 적는다.

## tips에 사례를 추가할 때

사례는 일반 조언이나 작업 성과 목록이 아니다. 실제로 관측한 문제와 이를 가른 증거가 있고 같은 실패를 다시 조사하는 비용을 줄일 때만 추가한다.

제목에는 다른 작업에서도 써먹을 수 있는 결론을 적는다. 본문에는 검색어, 관측 범위, 문제의 맥락, 원인을 가른 증거, 확인한 결과, 새 대상에서 다시 확인할 조건, 관련 전략 문서를 담는다. 타이틀명·주소·수치는 결론이나 적용 범위를 가르는 데 꼭 필요할 때만 남긴다.

파일 배치와 색인 형식은 `skills/create-kr-patch/references/tips/README.md`를 따른다.

## 구현과 검증

반복 빌드에 들어가는 구현은 입력, 출력, 원본 식별 정보와 실패 조건이 분명해야 한다. 원본에서 최종 산출물까지 같은 경로로 다시 실행할 수 있어야 하며, 현재 입력과 산출물에서 자동으로 알아낼 수 있는 오류는 제품 빌드가 막아야 한다.

조사는 아직 모르는 사실을 알아내고, 제품 빌드는 채택한 입력과 규칙으로 게임 산출물을 만든다. 테스트는 코드가 지켜야 할 동작을 확인한다. 산출물 검증은 만들어진 파일을, 런타임 검증은 게임이 그 결과를 실제로 사용하는 모습을 확인한다. 한 단계에서 얻은 결과를 다른 단계의 증거인 것처럼 말하지 않는다.

원본 ROM·디스크 이미지, 완성된 게임 이미지, 허가되지 않은 제3자 자산은 저장소와 배포물에 넣지 않는다.

문서 구조나 막혔을 때 되돌아보는 원칙을 크게 바꿨다면 현실적인 요청으로 에이전트의 행동도 확인한다. 국소 성공을 과장하지 않는지, 같은 자리를 맴돌 때도 유효한 증거는 보존하는지, 맞지 않는 사례나 특정 해법을 억지로 적용하지 않는지를 본다. 제목이나 문장, 문서 열람 순서를 그대로 재현하는지는 검증 대상이 아니다.

## 제출 전 확인

```bash
python3 scripts/validate_docs.py
python3 -m pytest -q scripts/test_validate_docs.py
```

마지막으로 다음을 확인한다.

- 새 내용이 실제 판단이나 검증에 도움이 되는가?
- 한 사례를 일반 규칙으로 확대하지 않았는가?
- 사람과 에이전트의 책임이 서로 뒤바뀌지 않았는가?
- 같은 기준이 다른 문서에 중복되지 않았는가?
- 참조가 실제 파일과 절을 가리키는가?
- 알려진 한계와 증거 범위를 숨기지 않았는가?
- 공개하면 안 되는 원본이나 내부 정보가 들어가지 않았는가?
