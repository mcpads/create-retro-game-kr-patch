# Changelog

이 프로젝트의 주요 변경 사항을 기록한다. 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고, 버전은 [유의적 버전](https://semver.org/lang/ko/)을 따른다.

## [Unreleased]

### Changed
- 자유 문장 초벌은 현재 에이전트나 같은 맥락을 받은 서브에이전트가 수행하고, 별도 모델·에이전트는 실제 대상 표본을 사람이 평가해 맡기기로 한 범위에서만 쓰도록 번역 작업의 경계를 보강.
- 대표 PoC와 다음 작업이 함께 성립해야 할 완성 조건과 확인된 제약 중 가장 까다로운 사례를 보존하고, 진단 결과를 원래 조건으로 돌려 판정하도록 작업 선택 규칙을 보강.
- 선언한 번역 범위의 사람 검수를 개발 통합과 분리하고 릴리스 후보의 조건으로 명시.
- tips를 최초 관측 플랫폼이 아니라 결론의 적용 범위에 따라 공통·플랫폼 고유 사례로 재배치하고, 플랫폼 코드 대신 결론을 말하는 제목과 영문 검색 필드를 사용하도록 정리.
- tips의 중첩 경로, 필수 사례 필드, 의미 기반 앵커와 strategy 포인터에서 유도한 판단 영역을 문서 검증에서 함께 확인하도록 보강.

## [1.2.1]

### Changed
- 선언한 현지화 범위에서 원문을 남기지 않는 경우, 원문 저장 영역과 기존 글리프·코드를 공간·폰트 공급 후보로 검토하되 참조가 남거나 원문 보존이 필요한 범위는 회수하지 않도록 판정 기준을 보강.
- 대규모 자산을 파일별로 실행 확인하지 않도록, 전체 목록은 정적으로 소비 경로별로 나누고 실행 검증은 서로 다른 경로·경계 조건·정적으로 판정할 수 없는 예외에 집중하도록 조사와 검증 기준을 보강.

### Fixed
- 폰트 코드 예약과 재배정, 원문을 남기지 않는 범위, 미확정 토큰의 배포 적격과 경계 보존에 관한 문서 사이의 모순을 해소.

## [1.2.1-alpha.2]

### Fixed
- 문서를 서로 대조해 읽어야 드러나는 모순을 해소. 폰트 코드 예약과 재배정을 잇달아 지시하던 문단, 정의 없는 "전량 현지화" 표현, 미확정 토큰의 배포 적격 조건이 한 문서에만 엄격하던 불일치, 토큰 경계 절단을 규약은 금지하고 사례는 해법으로 제시하던 어긋남을 정리. 조건은 이미 모두가 가리키는 절로 모아 상호 참조를 늘리지 않았다.

## [1.2.1-alpha.1]

### Changed
- 선언한 범위를 전량 현지화하면 원문 저장 영역과 원본이 쓰던 글리프·코드도 회수 후보가 된다는 점을 공간·폰트 판정에 명시. 기존 서술이 "미사용"으로 보이는 자원만 다뤄 최대 공급원이 후보에서 빠지고 공급 확장보다 번역 수요 축소로 기울던 문제를 해소.
- 회수 경계를 함께 규정: 참조 완전성이 입증된 범위를 단위로 삼고, 공유 tail·중복 포인터·문자열 내부 진입·인라인 리터럴·비텍스트 그래픽이 같은 자원을 소비하면 회수하지 않으며, 모집단이 미확정이거나 승인 예외로 원문을 남기는 범위와 수정 대상 밖에서 공급되는 글리프는 제외.

## [1.2.0]

### Added
- 텍스트 추출의 완료 조건을 다른 전략 문서처럼 문서 끝에서 한 번에 판정하는 절을 추가.
- `references/tips/README.md`의 색인 라벨과 `SKILL.md` 라우팅 판단 영역을 잇는 대응표를 추가.

### Changed
- 문서마다 다르게 답하던 네 규칙에 소유 문서를 하나씩 정함: 라운드트립 검증 분모, 실행 환경 지원 주장의 검증 범위, 완료 분모의 해결·제외·미확정 어휘, 유한한 표시 영역의 원본 사용 범위·확인된 소비 한도·채택한 표시 범위 구분.
- 분량 조사, 라운드트립 동등 기준, 휴리스틱 자동 채택 금지, 검증 증거 재사용 조건처럼 여러 문서에 재진술되던 규칙을 정본 한 곳과 포인터로 정리.
- `SKILL.md`의 스킬 발동 조건을 파이프라인 요약 대신 사용 시점으로 다시 쓰고, 핵심 불변식과 판단 흐름에서 겹치던 항목을 병합.
- `references/tips/README.md`의 판단 영역을 각 사례의 관련 판단 기준에서 유도하도록 정규화해 라벨 40종을 라우팅 판단 영역 11종으로 통일.
- 그래픽 텍스트 전략이 다른 횡단 문서와 같이 첫 절에서 발동 조건을 소유하도록 정리. 이 과정에서 `references/strategy/graphics-text.md` 첫 절의 제목이 「모집단 확정」에서 「발동 조건과 모집단」으로 바뀌고, `references/strategy/text-extraction.md`의 첫 절이 참조 대상에서 빠짐. 두 절을 외부에서 인용했다면 갱신이 필요하다.
- 번역 검수에서 보호 정보·인코딩·글리프와 확인된 표시 제약처럼 기계적으로 판정 가능한 위반만 자동 차단하고, 사전 등재·빈도·맞춤법·LLM 점수 등 언어 휴리스틱은 사람 검토 후보로만 사용하도록 자동화 경계를 명확히 함.
- 자유 문장을 규칙 기반 자동화로 생성·선택하지 않도록 번역 생성과 보조 자동화의 경계를 명확히 함.
- 영문 직역투 표현을 자연스러운 한국어로 정리하고, 기술 용어의 원어 표기는 유지.

### Fixed
- 주장과 다른 절을 가리키던 상호 참조 6건을 실제 소유 문서·절로 교정.

## [1.1.2-alpha.2]

### Changed

- 자유 문장을 규칙 기반 자동화로 생성·선택하지 않도록 번역 생성과 보조 자동화의 경계를 명확히 함.

## [1.1.2-alpha.1]

### Changed

- 번역 검수에서 보호 정보·인코딩·글리프와 확인된 표시 제약처럼 기계적으로 판정 가능한 위반만 자동 차단하고, 사전 등재·빈도·맞춤법·LLM 점수 등 언어 휴리스틱은 사람 검토 후보로만 사용하도록 자동화 경계를 명확히 함.

## [1.1.1]

### Changed
- 유한한 텍스트 표시 영역에서 원본이 실제로 사용한 범위와 확인된 표시 한도를 구분하고, 원문 최대값은 조사 근거로 활용하되 확정 한도나 자동 축약 근거로 삼지 않도록 초기 조사·PoC·번역·검증 흐름을 보강.

## [1.1.1-alpha.1]

### Changed
- 유한한 텍스트 표시 영역에서 원본의 사용 범위와 소비자가 허용하는 한도를 구분하고, 원문 최대값을 초기 증거로 활용하되 확정 한도나 자동 축약 근거로 삼지 않도록 초기 조사·PoC·번역·검증 판정을 보강.

## [1.1.0]

### Added
- 실행 검증의 목표 상태와 플레이 경로를 먼저 조사하고, 치트·상태 변경·확인된 상태 전이 루틴 호출을 재현 가능한 개입으로 기록하는 판정 기준을 추가.
- Nintendo DS NFTR의 태그 저장 순서와 CMAP 범위 중첩처럼 재발견 비용이 큰 사례를 적용 범위와 함께 복원.

### Changed
- 초기 조사와 PoC에서 선언한 완성 범위를 막거나 설계를 바꾸는 조건을 먼저 판정하고, 전수 열거 가능한 텍스트는 초기에 전체 분량과 미확정 범위를 함께 파악하도록 보강.
- PoC에서 채택한 폰트·인코딩·재삽입·코드 변경을 주 빌드로 회수하고, 불변 원본에서 함께 재생성한 하나의 개발 빌드로 통합하도록 보강. 미완료 번역은 원문 유지 상태로 함께 검증.
- 가시성 PoC부터 주요 문자 집합은 기성 글꼴로 공급하고, 필수 글리프의 국소 누락이나 확인된 UX 요구가 있는 범위만 직접 제작·보정하도록 선택 경계를 강화.
- 번역 일괄 변경의 사람 승인과 Expected Write의 쓰기 주체, 선언한 ISA 프로필의 조립·해석 검산을 핵심 불변식과 프로젝트 규약에 일치시킴.
- `SKILL.md`와 참조문서의 중복 포인터를 줄이고, 현재 판단과 관측 증상에 맞는 전략·플랫폼 제약·tips만 읽도록 라우팅을 정리.

## [1.1.0-alpha.3]

### Changed
- 초기 조사와 PoC가 쉬운 부분 성공보다 선언한 완성 범위를 막거나 설계를 크게 바꾸는 미확정 조건을 먼저 판정하고, 같은 조건을 가르는 증거 사이에서만 비용을 비교하도록 보강.
- `SKILL.md`와 참조문서가 같은 기준을 반복하거나 포괄적으로 다른 문서를 가리키던 부분을 발동 조건·변경 경계·사례별 판단 기준으로 좁히고, 플랫폼별 사례 선택은 `references/tips/README.md` 한 곳으로 통합.
- Expected Write의 단일 변경 책임을 `쓰기 주체`로 명확히 표현하고, PoC 기록의 선택 필드 예시를 `poc_id`로 정리.
- 번역 일괄 변경의 사람 승인을 핵심 불변식에 연결하고, NDS NFTR의 태그 저장 바이트 순서와 CMAP 범위 중첩 사례를 확인된 범위와 전이 한계로 복원.

## [1.1.0-alpha.2]

### Changed
- 초기 조사·PoC에서 전수 열거 가능한 텍스트 경계를 확보하면 표본에서 멈추지 않고 전체 분량을 조사하며, 번역 작업을 전체 배포 범위로 확대하기 전에 확정값·하한·추정치와 미확정 모집단을 구분하도록 보강.
- 문서 전반에서 여러 의미를 대신하던 "게이트", 승인 SSOT와 모집단의 열림·닫힘 은유를 판정·검증·검사, 승인 기준과 미확정·확정으로 구체화.

## [1.1.0-alpha.1]

### Added
- 실행 검증의 목표 상태와 플레이 경로가 미확정이면 조작법·진행 흐름을 먼저 조사하고, 필요한 선행 상태를 보존하는 도달 수단 중 재현 비용이 가장 낮은 경로를 고르는 판정 기준을 추가.
- 치트·상태값 변경·확인된 상태 전이 루틴 호출의 사전 확인 조건, 증명 범위와 기록 의미를 정하고 조사용 개입을 패치 내용과 반복 빌드 입력에서 분리.

## [1.0.1]

### Changed
- `SKILL.md`의 왕복 검증·최종 변경 검증·기계어 검산을 능력과 통과 조건 중심으로 간결화하고, 구체 시행 방식과 제한된 예외는 `references/conventions/project-conventions.md`가 소유하도록 정리.
- 중복된 PoC·사례 수치 설명은 해당 전략 문서를 따르게 하고, 최상위 안내의 불필요한 영문과 내부 약어를 자연스러운 한국어로 정리.
- 빌드·재삽입 전략이 구체 구현 명칭 대신 최종 변경의 검증 결과를 가리키도록 맞추고, 새 라운드트립 절 포인터를 문서 검증 대상에 등록.

## [1.0.0]

### Added
- 구현·데이터·기록·번역 자산의 의미와 검증 규칙을 분리한 `references/conventions/` 문서와 저장부터 실제 소비까지 추적하는 런타임 자산 전략을 추가.
- 관측 증상으로 필요한 항목만 고르는 `references/tips/` 색인에 검증된 실패·성공 기법·운영 함정과 어려운 조사 결과를 전이 한계와 함께 보존.
- 문서 경로·절 참조·tips 색인 대응과 기대 제목을 검사하는 저장소 검증기를 추가.

### Changed
- `SKILL.md`는 핵심 불변식과 문서 안내, 전략 문서는 조건부 판단 기준, 플랫폼 문서는 실제 분기를 바꾸는 사실을 맡도록 책임을 재구성하고 중복을 정리.
- 고정된 단계 대신 현재 판단을 바꾸는 증거부터 조사하고, 새 증거가 나오면 영향받은 판정만 되돌리는 반복 흐름으로 전환.
- 무변경 왕복, 선언한 ISA 프로필의 기계어 검산, Expected Write의 단일 소유와 전체 diff 감사, 런타임 소비 증거를 공통 검증 경계로 강화.
- 보호된 원문과 가역 토큰을 쓰는 Agent 번역 배치, 독립 검토와 사람 승인, 일괄 변환 사전 승인, 글리프 공급과 목표 렌더 경로 확인 뒤의 전체 사람 검수를 하나의 번역 판정 흐름으로 연결.
- 검증 가능한 기존 글꼴을 먼저 평가하고 필요한 글리프만 제작·보정하며, 그래픽 텍스트·창·상태·제어코드와 선행 현지화 자료를 실제 소비 범위에서 활용하도록 정리.

### Removed
- 특정 도구·언어·라이브러리·폰트와 한 프로젝트의 주소·수치·절차를 공통 규칙처럼 지시하던 내용을 제거.
- 전략과 사례를 섞거나 같은 판단 기준을 반복하던 기존 문서와 플랫폼별 사례 요약을 통합·분리.

## [1.0.0-alpha.3]

### Added
- 상태별 폰트 자산이 게임의 원래 적재 경로를 거쳐 실제 화면이 사용하는 VRAM에 도달한 Saturn 사례를 추가.
- 크기가 늘어난 압축 자산에 맞춰 고정 해제 크기를 보정한 Saturn 사례를 추가.
- 확인된 직접 포인터 하나가 파일 끝에 추가한 문자열을 실제로 읽는 데까지 검증하고, 선택창 동작과는 구분한 Saturn 사례를 추가.
- 같은 선택지 표시기를 쓰는 대표 화면에서 파일 끝에 추가한 문자열의 적재 크기, 창 크기, 전체 표시와 선택 동작까지 검증한 Saturn 사례를 추가.
- 선행 NES 패치가 실제로 사용하는 PRG-RAM과 이미지 헤더 선언의 불일치를 분석용 사본에서 분리한 사례를 추가.
- 포인터 표를 다시 읽지 않고 현재 객체 포인터를 다음 블록까지 이어 쓰는 SNES 경로를 추가.
- RAM 재사용 뒤 압축 역참조를 문자열 읽기로 오인한 Saturn 사례를 추가.

### Changed
- 초벌 번역에서 드러난 글리프 수요를 현재 설계와 실제 표시 경로가 감당할 수 있는지 확인한 뒤 전체 번역의 사람 검수·최종 다듬기를 시작하도록 판정 시점을 명시.
- 자동 검사는 번역 결함 후보와 영향 범위만 만들고, 규칙 기반 일괄 변환은 기준선·규칙·범위와 영향을 사람이 명시적으로 승인한 뒤에만 적용하며 결과 검증을 생략하지 않도록 번역 수정 경계를 명시.
- 자체 기계어 생성·해석은 선언한 ISA 프로필 전체를 양방향 검증하고, 모든 최종 변경은 불변 원본 기준 Expected Write 사전 계획과 단일 소유·전체 diff 검사를 통과하도록 강화.
- 검증 가능한 기성 글꼴을 먼저 평가하고 조정으로 충족하지 못한 글리프만 필요한 범위에서 제작·보정하도록 폰트 선택 순서를 명확화.
- 새 판단 영역이나 관측 변화마다 tips 색인을 발동 조건과 대조하고, 일치하는 사례 앵커만 조사·개입 설계 전에 읽도록 라우팅을 보강.
- 국소 사례에서 관측 범위·수치를 전이 한계에 되풀이하던 문장을 걷어내고 재검증 조건 중심으로 정리.
- 빈 공간 오판, 공유 글리프 슬롯, 헤더 없는 그래픽의 형식 판정, 고정 슬롯 패딩, 바이트 모양 기반 참조 오판처럼 같은 분류 오류와 판정 경계를 다루던 tips를 플랫폼 간 사례로 통합.
- tips 색인을 관측 가능한 증상 중심으로 고치고, 결론에 필요하지 않은 도구·버전·프로젝트 내부 수치·파일명·토큰 표기·진행 상태와 불필요한 영문 직역을 정리.

## [1.0.0-alpha.2]

### Added
- 실제 한글화 프로젝트에서 소비 경로까지 입증된 드문 성공 기법과 버전·입력 범위가 확인된 운영 함정을 선택형 검증 사례로 복원.
- 시각 효과·대사창·선택 상태와 헤더 없는 자산의 bpp·폭·오프셋 판정을 실제 범위에 한정한 UX·그래픽 사례로 보강.
- Nintendo DS 이름 입력 화면의 복수 자산 공급 경로와 내부 폰트 엔트리 경계를 대조 실험 범위에 한정한 사례로 보강.
- 문서 참조의 파일·앵커·절 존재뿐 아니라 절 번호가 가리키는 기대 제목과 tips 색인 대응까지 검사하는 저장소 검증을 추가.

### Changed
- `references/tips/`를 실패 전용 단서에서 실패·성공 기법·어려운 조사 결과를 함께 보존하는 비규범적 사례 티어로 확장.
- 에이전트용 참조문서의 기여자 배치 지침을 제거하고 실제 판정·행동 경계로 정리.
- 글리프 수요 축소를 빈도가 아닌 실제로 줄어드는 글리프 수·적용 범위·의미 손실로 판정하고, Agent 번역 배치에 대상 엔진의 토큰별 소비 의미와 처리 정책을 포함하도록 보강.
- 선행 현지화 자료를 언어 이름보다 현재 병목의 유사성으로 선택하고, 중문·한자권 패치의 폰트·인코딩·자원 확장 해법을 한글 수요에서 재검증하도록 보강.
- 문서 라우팅의 단계 표현을 판단 영역으로 바꾸고, 독립 경계의 병렬 조사와 영향받은 판정만 되돌리는 반복 구조를 명시.
- 런타임 자산 발동 조건, 패딩 프로필 범위, 번역 배치·빌드 적격, Expected Write의 문서 소유와 적용 범위를 서로 일치하도록 정리.
- 기계 판독 입출력의 공통 요건은 프로젝트 시행 컨벤션만 소유하게 하고, 분석·빌드 데이터와 번역 자산 문서에는 도메인별 의미와 포인터만 남김.
- 글리프·그래픽 텍스트·재삽입·최종 QA가 실제 배경의 시각 기준, 창·상태·입력·진행과 전환 뒤 잔상을 변경 영향에 따라 판정하도록 연결.
- 헤더나 확정 소비자가 없는 그래픽 자산은 파일 크기만으로 추정하지 않고 수정 영역의 픽셀 주소 규칙과 경계를 확정하며, 선형 비트맵에서 비트 깊이·시작 오프셋·행 간격·폭을 함께 판정하도록 보강.
- 소비 의미가 확정된 줄·페이지·대기 제어 토큰을 한국어의 판독성과 진행 호흡에 맞춰 재배치할 수 있게 하고, 초상·창·입력·음성·이벤트 상태를 바꾸는 토큰은 확인된 허용 범위와 최종 소비 결과로 제한.

## [1.0.0-alpha.1]

### Added
- 구현·데이터·기록의 의미와 검증 규칙을 소유하는 `references/conventions/` 티어와 문서 간 명시 포인터 규칙.
- 저장부터 실제 소비까지의 연결을 판정하는 조건부 런타임 자산 전략.
- 플랫폼·증상·적용 한계로 선택해 읽는 비규범적 `references/tips/` 사례 모음.

### Changed
- `SKILL.md`를 라우터와 핵심 불변식 중심으로 줄이고, 전략은 조건부 게이트와 판정 기준 중심으로 재정리.
- 고정 ROM 리비전에서 확인된 주소·크기·개수는 명시 사양으로 다루고 휴리스틱은 조사·감사에 한정하도록 명문화.
- 플랫폼 문서를 타이틀별 시행 절차가 아니라 실제 분기를 바꾸는 하드웨어·매체·런타임 사실과 적용 범위 중심으로 재구성.
- 보호된 raw 기준선과 가역 토큰을 쓰는 Agent 번역 배치, 독립 2차 LLM 리뷰와 사람 승인 흐름을 도구 독립적으로 보존.
- Expected Write, 생성 기계어 재해석 검산, 전체 diff 감사를 반복 프로젝트에서 확인된 공통 검증 요건으로 명문화.

### Removed
- 특정 도구·언어·라이브러리 선택을 공통 규약처럼 지시하던 구현 지침.
- 전략과 사례를 한 파일에 섞던 기존 단일 `strategy/tips.md`.

## [0.7.0]

### Changed
- 선행 패치 참조 전략을 영문 패치 중심에서 타국어·선행 현지화 패치 전반으로 일반화. 원본→타국어 패치→한국어 경로에서 생길 수 있는 뉘앙스 손실, 중문 패치의 폰트 확장·8x8 가독성 참조 경계, 원본 네이티브 설계로 전환하는 조건을 명확화.
- 문서 간 참조 포인터와 SSOT 라우팅을 전사적으로 점검. 선행 패치·레이아웃·그래픽 텍스트·빌드 QA 관련 참조를 소유 문서로 돌리고, 축약 포인터를 명시 경로로 정리.
- KR 문서 어감에 맞지 않는 은유적 "닫는다" 표현을 통과·검증·확정·판정·종료·해소로 의미별 정리하고, 실제 UI/문자 동작 용례만 유지.

## [0.6.0]

### Changed
- 자동 검증을 번역 파이프라인의 1회 관문이 아니라 사람 판단 사슬과 직교하는 **상시 빌드 게이트**로 재배치(`references/strategy/build-and-verify.md` §1). 클린 플레이트 재현성 기준을 결정적/비결정적 복원으로 분리하고, 비결정적 플레이트에 해시 오라클을 도입해 파생물이 재현 불가한 기준 자료가 되지 않게 함.
- SSOT 중복 통합 — CD EDC/ECC 스펙(반전 상수 포함), FPGA 검증 티어, ROM 배포 정책, 포인터 검출 오류 유형론, 인코딩 누락 엔진 메커니즘 등을 소유 문서로 모으고 나머지는 포인터화.
- 플랫폼 문서를 한 게임·특정 도구 편향에서 하드웨어·포맷 사실로 정리 — 미검증 단일 사례의 규칙화(터치 QA), 한 게임 구현 세부(GG 영문 팬패치 기반 VWF·인코딩 대역 서술 등), 특정 에뮬레이터·API 강요, 에뮬레이터 전용 최적화, 프로젝트 상태 서술을 제거. 범용 전략(선행 패치 활용)과 검증 원칙(관대한 에뮬레이터 통과 ≠ 정밀·실기 통과)은 전략 티어로 참조·격리하고, 과일반화를 조건부로 완화.

### Fixed
- 사실오류 정정 — NDS ARM9 BIOS 크기(32KB)·DTCM 기본 base(툴체인 편차 명시), `tips`의 x86 D 플래그(방향 플래그이지 BCD 아님), BPS CRC32 3중(원본·결과·패치), PS1 Mode 2 Form 1 섹터 구분, `references/strategy/build-and-verify.md` §1 참조 앵커.

### Added
- NDS UTF-8 게임 절(일본어·한글 모두 3바이트라 JP→KR 길이 중립), NFTR 온디스크 역순 태그·CMAP first-match prepend 조건.
- `tips`에 헤더 없는 그래픽·폰트 자산의 포맷 판별 단서(폭·bpp·색 수·offset, 2의 거듭제곱 편향 경계).

## [0.5.0]

### Added
- 기여 철학과 문서 작성 기준을 담은 `CONTRIBUTING.md` 추가 — 사람 개입 축소·Agent 자율 판단·재현성을 기여 기준으로 명문화하고 문서 티어 규칙을 정리.
- `tips`에 헤더 없는 그래픽·폰트 자산의 포맷 판별 단서 보강 — 폭·bpp·색 수를 크기 인수분해와 컨택트시트로 좁히고 2의 거듭제곱 편향을 경계하는 절, 한 파일에 폭·offset·bpp가 섞이거나 슬롯→offset이 비선형일 때의 섹션 분해 절. 방법론 본체는 `graphics-text`·`font-strategy`로 라우팅.

### Changed
- PoC의 방향을 "닫기 쉬운 저위험 증명"이 아니라 배포 목표(0원칙)를 가장 크게 위협하는 블로커를 먼저 직면하는 탐사로 명문화 — `SKILL.md` 핵심 불변식, `poc.md`, `CONTRIBUTING.md` 전 티어에 일관 반영.
- 플랫폼 참조 문서의 적용 범위 주의(caveat) 정정.

## [0.4.0]

### Changed
- PoC 문서를 게이트 묶음 중심으로 재정리 — 한글 1자 출력이 인코딩 공간·글리프 수용량·재배치 가능성을 자동 증명하지 않는다는 한계를 명시하고, 저장 글리프 예산·활성 메모리/VRAM·슬롯 회수·재배치·압축·그래픽 텍스트·레이아웃·이름 입력을 조건부 PoC 판정표로 분리.
- 글리프 수요/공급 판단을 PoC의 확정 판정에서 `font-strategy`의 실제 빌드 게이트와 수요 조정 절로 이동 — 표본 단계는 초과 위험과 후보 정책을 남기고, 실제 미할당 처리는 전수 집계와 빈도표 기반 조정 루프에서 다루도록 정리.
- `SKILL.md`와 전략 문서 전반의 명칭을 Claude Code Skill 중심 표현에서 Agent Skill 중심 표현으로 정리.
- 작업 단위의 판정성 원칙 추가 — 조사·PoC·번역 배치·검증 결과가 후속 선택지를 줄이고 다음 판단을 더 정확하게 만들어야 한다는 전역 불변식과 PoC/초기 조사 산출물 기준 보강.

## [0.3.0]

### Added
- 닌텐도 DS 플랫폼 문서(`references/platforms/nds.md`) 추가 — ARM9/ARM7, NitroFS, 인코딩·렌더 경로 등. `SKILL.md` 라우팅 테이블에 NDS 항목 추가. (mirusu400, PR #1)
- 복수 렌더 타깃의 폰트 설정 외부화 패턴 — 타깃별 폰트 프로파일을 타입 검증되는 설정으로 분리하되, 단일 폰트 프로젝트에는 과설계라는 경계를 명시. 현재 판단 기준은 `references/strategy/font-strategy.md` §5, 데이터 의미는 `references/conventions/data-formats.md` §6에서 소유한다.

## [0.2.0]

### Changed
- 사례 실적 과시 수치(포인터·글리프·바이트 예산 등)와 "제작자 확인" 류 빈권위 표현을 제거하고 교훈 중심으로 정렬.
- 원칙·교훈 섹션의 비공개 게임 일화를 사례성 표현("한 사례에서는 …")으로 일반화. 일반화 과정에서 단일 사례가 빈도·보편 주장으로 격상되지 않도록 전수 교정. 공식·인코딩 경계·코드·"사례 요약" 섹션은 보존.

### Added
- 실패→원인→해법 디버깅 교훈 보강 (`tips`, `debugging`).
- 글리프 예산 입력값 측정 절차 보강 (`font-strategy`, `text-extraction`).
- 검증 자동화의 선행 비용·한계 경고 + 검증 인프라를 작업 시점에 조사·개조하는 방향 (`build-and-verify`).
- 오역=블로커를 강제하는 자동 검출 게이트 보강 (`translation-workflow`).
- 의도적 EDC/ECC 오류(카피 프로텍션) 정정 금지 경고 (`build-and-verify`, `saturn`, `ps1`).
- 레이아웃을 글자 폭 테이블로 분리하는 설계 옵션 (`reinsertion`).

## [0.1.0]

### Added
- 레트로 게임 한글 패치 제작 Agent Skill 최초 공개.
- 단계별 공통 전략 문서 12종 (`references/strategy/`): 초기 조사, 폰트·인코딩, 텍스트 추출, PoC, 재삽입·훅, 번역, 빌드·검증, 디버깅, 그래픽 텍스트, 압축, 저장소 규약, 실전 팁.
- 플랫폼별 하드웨어·사례 노트 8종 (`references/platforms/`): SNES, 메가드라이브, 새턴, PS1, 드림캐스트, PC엔진, PC-98, 게임기어.
- Agent Skill 플러그인 매니페스트 및 셀프호스팅 마켓플레이스 구성.
- MIT 라이선스로 공개.
